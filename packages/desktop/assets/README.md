Drop a Windows `icon.ico` (multi-resolution: 16/32/48/256px) here before
building a real release, then uncomment the `icon:` line in
`../electron-builder.yml`. Until then, the packaged app uses Electron's
default icon.
