{
  "name": "life-manager",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=@life-manager/backend",
    "dev:frontend": "npm run dev --workspace=@life-manager/frontend",
    "dev:desktop": "npm run start --workspace=@life-manager/desktop",
    "build": "npm run build --workspaces",
    "build:desktop": "npm run dist --workspace=@life-manager/desktop",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.14.0",
    "vitest": "^2.1.4"
  }
}
